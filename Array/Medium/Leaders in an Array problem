#include <bits/stdc++.h>
using namespace std;
vector<int> leaders(vector<int> &arr)
{
    // code here
    int n = arr.size();
    vector<int> ans;
    for (int i = n - 1; i >= 0; i--)
    {
        if (i == n - 1)
        {
            ans.push_back(arr[i]);
        }
        else if (arr[i] >= ans.back())
        {
            ans.push_back(arr[i]);
        }
    }
    reverse(ans.begin(), ans.end());
    return ans;
}
int main()
{
    int n;
    cin >> n;
    vector<int> arr1(n);
    // vector<int> arr2(n);
    for (int i = 0; i < n; i++)
    {
        cin >> arr1[i];
    }
    // int k;
    // cin >> k;
    vector<int> ans = leaders(arr1);
    // cout << ans << endl;
    for (int i = 0; i < ans.size(); i++)
    {
        cout << ans[i] << " ";
    }

    return 0;
}
